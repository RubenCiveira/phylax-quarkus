<!--
  CI profile: format
  Runs code formatter (google-style) + import sorter.
  Activated by: mvn formatter:format impsort:sort -Pci-format

  Owned by CI team — do not add to pom.xml profiles.
-->
<profile>
  <id>ci-format</id>
  <build>
    <plugins>
      <plugin>
        <groupId>net.revelc.code.formatter</groupId>
        <artifactId>formatter-maven-plugin</artifactId>
        <version>2.13.0</version>
        <configuration>
          <configFile>${project.basedir}/src/eclipse-java-google-style.xml</configFile>
          <lineEnding>LF</lineEnding>
          <directories>
            <directory>${project.basedir}/src/main/java</directory>
            <directory>${project.basedir}/src/test/java</directory>
            <directory>${project.basedir}/target/generated-sources</directory>
          </directories>
        </configuration>
      </plugin>
      <plugin>
        <groupId>net.revelc.code</groupId>
        <artifactId>impsort-maven-plugin</artifactId>
        <version>1.13.0</version>
        <configuration>
          <groups>java.,javax.,org.,com.</groups>
          <staticGroups>java,*</staticGroups>
          <removeUnused>true</removeUnused>
          <directories>
            <directory>${project.basedir}/src/main/java</directory>
            <directory>${project.basedir}/src/test/java</directory>
            <directory>${project.basedir}/target/generated-sources</directory>
          </directories>
        </configuration>
      </plugin>
    </plugins>
  </build>
</profile>
