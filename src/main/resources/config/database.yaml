# @autogenerated
quarkus:
  datasource:
    db-kind: mysql
     # test_read luego, por ahora test_write
    username: ${datasource.user:}
    password: ${datasource.pass:}
    jdbc:
      url: jdbc:mysql://${datasource.host:localhost}:${datasource.port:5432}/${datasource.path:5432}
      max-size: 1
      min-size: 1

app:
  migration:
    db-kind: mysql
    username: ${datasource.user:}
    password: ${datasource.pass:}
    group: mysql
    location: db/migration/mysql/changelog.txt
    jdbc:
      url: jdbc:mysql://${datasource.host:localhost}:${datasource.port:5432}/${datasource.path:5432}
