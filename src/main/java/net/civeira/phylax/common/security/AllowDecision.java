/* @autogenerated */
package net.civeira.phylax.common.security;

import java.util.function.Function;

import lombok.Data;
import lombok.NonNull;
import lombok.experimental.SuperBuilder;

@Data
@SuperBuilder(toBuilder = true)
public abstract class AllowDecision {

  @NonNull
  private Allow detail;

  @NonNull
  public final Interaction query;

  public abstract String resourceName();

  public abstract String actionName();

  public void map(Function<Allow, Allow> allowed) {
    detail = allowed.apply(detail);
  }

  public void deny() {
    this.detail.setAllowed(false);
  }

  public void deny(String reason) {
    this.detail.setAllowed(false);
    this.detail.setDescription(reason);
  }
}
