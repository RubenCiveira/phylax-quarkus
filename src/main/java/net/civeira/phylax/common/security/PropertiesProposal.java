/* @autogenerated */
package net.civeira.phylax.common.security;

import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

import lombok.Getter;

@Getter
public abstract class PropertiesProposal {

  private Set<String> fields;

  public final OperationContext context;

  public PropertiesProposal(OperationContext context) {
    super();
    this.fields = new HashSet<>();
    this.context = context;
  }

  public PropertiesProposal(OperationContext context, Set<String> fields) {
    super();
    this.fields = fields;
    this.context = context;
  }

  public abstract String resourceName();

  public abstract String viewName();

  public Actor getActor() {
    return context.getActor();
  }

  public InvocationSource getSource() {
    return context.getSource();
  }

  public void add(String... newfields) {
    add(Arrays.asList(newfields));
  }

  public void add(List<String> newfields) {
    try {
      fields.addAll(newfields);
    } catch (UnsupportedOperationException ex) {
      fields = new HashSet<>(newfields);
    }
  }
}
