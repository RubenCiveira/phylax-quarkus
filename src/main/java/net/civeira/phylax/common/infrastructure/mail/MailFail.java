/* autogenerated */
package net.civeira.phylax.common.infrastructure.mail;

/**
 * Callback for failed email deliveries.
 *
 * Implementations can record errors, trigger retries, or notify callers. The exception contains the
 * underlying failure cause from the mailer. This interface is used by {@link EmailMessage} and
 * invoked by {@link EmailService}. Keep implementations lightweight to avoid blocking send threads.
 */
public interface MailFail {
  /**
   * Invoked when an email delivery fails.
   *
   * The exception may wrap transport errors or configuration issues. Implementations should avoid
   * throwing to prevent masking failures. Use this to record metrics or alert operators.
   *
   * @param ex delivery failure
   */
  void fail(Exception ex);
}
