/* @autogenerated */
package net.civeira.phylax.common.infrastructure.store;

import java.io.InputStream;

import lombok.Builder;
import lombok.Getter;

/**
 * Represents a binary file or content stored in an input stream.
 * <p>
 * This class is designed to hold the binary content along with metadata such as content type, name,
 * and last modification timestamp.
 * </p>
 */
@Getter
@Builder
public class BinaryContent {

  /**
   * The input stream containing the binary data.
   */
  private final InputStream inputStream;

  /**
   * The MIME type of the content (e.g., "image/png", "application/pdf").
   */
  private final String contentType;

  /**
   * The name of the file or content.
   */
  private final String name;

  /**
   * The timestamp of the last modification in milliseconds since the epoch.
   */
  private final Long lastModification;
}
