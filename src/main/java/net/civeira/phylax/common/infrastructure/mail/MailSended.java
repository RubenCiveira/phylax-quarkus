/* autogenerated */
package net.civeira.phylax.common.infrastructure.mail;

/**
 * Callback for successful email deliveries.
 *
 * Implementations can update state, trigger workflows, or log delivery success. This interface is
 * used by {@link EmailMessage} and invoked by {@link EmailService}. It provides a simple signal for
 * completion without additional payloads. Keep implementations lightweight to avoid blocking send
 * threads.
 */
public interface MailSended {
  /**
   * Invoked when an email is delivered successfully.
   *
   * This is called after the mailer reports a successful send. Implementations should avoid
   * throwing to prevent masking success. Use it to update status or trigger follow-up logic.
   */
  void ok();
}
