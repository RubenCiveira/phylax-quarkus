/* @autogenerated */
package net.civeira.phylax.common.batch;

import lombok.Builder;
import lombok.Getter;

/**
 * Represents a named executable unit in a batch execution plan.
 *
 * <p>
 * This class binds together the name of a step, the executor implementation, and the input
 * parameters required to run it.
 * </p>
 *
 * @param <T> the type of parameters passed to the executor
 */
@Getter
@Builder
public class ExecutorPlan<T> {
  /** Step name. */
  private String name;

  /** Executor implementation for the step. */
  private Executor<T> executor;

  /** Parameters passed to the executor. */
  private T params;
}
