/* @autogenerated */
package net.civeira.phylax.common.batch.stepper;

/**
 * Defines logic to initialize a batch step before item processing begins.
 *
 * <p>
 * This interface can be used to prepare necessary state, validate parameters, allocate resources,
 * or perform any setup logic needed before reading and processing items.
 * </p>
 *
 * @param <P> the type of step parameters
 * @param <S> the type of step state
 */
public interface StepInitializer<P, S> {
  /**
   * Initializes the step context before execution.
   *
   * @param context the current step context
   */
  void init(StepContext<P, S> context);
}
