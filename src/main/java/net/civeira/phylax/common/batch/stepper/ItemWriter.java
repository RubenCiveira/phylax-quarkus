/* @autogenerated */
package net.civeira.phylax.common.batch.stepper;

import java.util.List;

/**
 * Defines the logic for writing processed items at the end of a batch step.
 *
 * <p>
 * The {@code ItemWriter} is responsible for storing, sending, or otherwise finalizing the result of
 * processed items (e.g., saving to a database, writing to a file, sending events).
 * </p>
 *
 * @param <R> the type of items to write
 * @param <P> the type of step parameters
 * @param <S> the type of step state
 */
public interface ItemWriter<R, P, S> {
  /**
   * Writes the list of processed items.
   *
   * @param item the list of results to write
   * @param context the current step context
   */
  void write(List<R> item, StepContext<P, S> context);
}
