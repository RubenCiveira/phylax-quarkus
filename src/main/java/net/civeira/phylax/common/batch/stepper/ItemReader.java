/* @autogenerated */
package net.civeira.phylax.common.batch.stepper;

import java.util.List;

/**
 * Defines the logic for reading items at the beginning of a batch step.
 *
 * <p>
 * The {@code ItemReader} is responsible for providing the initial data to be processed by the step.
 * This may involve reading from a database, file, API, or other sources.
 * </p>
 *
 * @param <T> the type of items read
 * @param <P> the type of step parameters
 * @param <S> the type of step state
 */
public interface ItemReader<T, P, S> {
  /**
   * Reads a list of items for processing.
   *
   * @param context the current step context
   * @return a list of items to be processed
   */
  List<T> read(StepContext<P, S> context);
}
