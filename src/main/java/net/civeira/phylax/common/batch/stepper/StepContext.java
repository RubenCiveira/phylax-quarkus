/* @autogenerated */
package net.civeira.phylax.common.batch.stepper;

import lombok.Data;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.common.batch.Monitor;

/**
 * Holds contextual information for a single execution of a batch step.
 *
 * <p>
 * The {@code StepContext} provides access to step parameters, monitoring tools, and mutable state
 * that can be updated during processing.
 * </p>
 *
 * @param <P> the type of parameters passed to the step
 * @param <S> the type of mutable state associated with the step
 */
@Data
@RequiredArgsConstructor
public class StepContext<P, S> {
  /** Parameters passed to the step. */
  private final P params;

  /** Monitor for tracking progress, logging, and runtime observability. */
  private final Monitor monitor;

  /** Mutable state that can be updated during the step execution. */
  private S state;
}
