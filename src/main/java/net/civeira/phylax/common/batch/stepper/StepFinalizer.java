/* @autogenerated */
package net.civeira.phylax.common.batch.stepper;

/**
 * Defines logic to execute after a batch step has completed.
 *
 * <p>
 * This can include final state cleanup, resource release, notifications, or post-processing
 * activities that should occur after all items have been handled.
 * </p>
 *
 * @param <P> the type of step parameters
 * @param <S> the type of step state
 */
public interface StepFinalizer<P, S> {
  /**
   * Executes finalization logic after the step has completed.
   *
   * @param context the current step context
   */
  void finish(StepContext<P, S> context);
}
