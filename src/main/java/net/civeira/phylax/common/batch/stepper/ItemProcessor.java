/* @autogenerated */
package net.civeira.phylax.common.batch.stepper;

/**
 * Defines the transformation logic for individual items in a batch process.
 *
 * <p>
 * This interface is responsible for converting or processing items from one type to another during
 * the execution of a step (e.g., validation, enrichment, mapping).
 * </p>
 *
 * @param <T> the input item type
 * @param <R> the result/output type
 * @param <P> the type of step parameters
 * @param <S> the type of step state
 */
public interface ItemProcessor<T, R, P, S> {
  /**
   * Processes an individual item within the step context.
   *
   * @param item the input item to process
   * @param context the current step context
   * @return the transformed result
   */
  R process(T item, StepContext<P, S> context);
}
