/* @autogenerated */
package net.civeira.phylax.common.batch;

import jakarta.enterprise.context.ApplicationScoped;

/**
 * Default CDI implementation of {@link SleepService} that delegates to {@link Thread#sleep(long)}.
 *
 * <p>
 * This class provides real-time delay behavior and is suitable for use in production code. It is
 * registered as an {@code @ApplicationScoped} CDI bean so that any {@link Executor} implementation
 * can inject it when rate-limiting or throttling between processing steps.
 * </p>
 */
@ApplicationScoped
public class RealSleepService implements SleepService {

  /**
   * Sleeps the current thread for the given number of milliseconds.
   *
   * @param millis the duration in milliseconds
   * @throws InterruptedException if the thread is interrupted during sleep
   */
  @Override
  public void sleep(long millis) throws InterruptedException {
    Thread.sleep(millis);
  }
}
