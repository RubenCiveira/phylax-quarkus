/* @autogenerated */
package net.civeira.phylax.common.batch.stepper;

/**
 * Provides a human-readable description for a given item within a batch step.
 *
 * <p>
 * This interface is used to log or display contextual information about each item being processed.
 * It helps identify items in logs, error messages, or monitoring dashboards.
 * </p>
 *
 * @param <T> the type of the input item
 * @param <P> the type of parameters used by the batch step
 * @param <S> the type of state maintained by the step
 */
public interface ItemDescriptor<T, P, S> {

  /**
   * Returns a textual description of the given item.
   *
   * @param item the item to describe
   * @param context the current step context
   * @return a description string
   */
  String itemDescription(T item, StepContext<P, S> context);
}
