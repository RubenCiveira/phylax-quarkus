package net.civeira.phylax.features.access.useracceptedtermnsofuse.transition.create;

import java.util.ArrayList;
import java.util.List;
import java.util.function.Function;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.enterprise.inject.Instance;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.features.access.useracceptedtermnsofuse.UserAcceptedTermnsOfUse;
import net.civeira.phylax.features.access.useracceptedtermnsofuse.command.UserAcceptedTermnsOfUseWriteAttributes;
import net.civeira.phylax.features.access.useracceptedtermnsofuse.transition.UserAcceptedTermnsOfUseChangeProposal;

@ApplicationScoped
@RequiredArgsConstructor
public class UserAcceptedTermnsOfUseCreatePipeline {

  /**
   * @autogenerated UpdateEntityRuleGenerator
   */
  private final Instance<UserAcceptedTermnsOfUseCreateStep> steps;

  /**
   * @autogenerated UpdateEntityRuleGenerator
   * @param attributes
   * @param last
   * @return
   */
  public UserAcceptedTermnsOfUse create(final UserAcceptedTermnsOfUseWriteAttributes attributes,
      final Function<UserAcceptedTermnsOfUseChangeProposal, UserAcceptedTermnsOfUse> last) {
    Function<UserAcceptedTermnsOfUseCreateTransition, UserAcceptedTermnsOfUse> finalHandler = t -> {
      if (t.hasErrors()) {
        throw t.asConstraintException();
      }
      return last.apply(t.getData());
    };
    List<UserAcceptedTermnsOfUseCreateStep> reversed =
        new ArrayList<>(steps.stream().toList()).reversed();
    Function<UserAcceptedTermnsOfUseCreateTransition, UserAcceptedTermnsOfUse> pipeline =
        reversed.stream().reduce(finalHandler,
            (next, step) -> transition -> step.create(transition, next), (f1, f2) -> f1);
    return pipeline.apply(UserAcceptedTermnsOfUseCreateTransition.builder()
        .data(new UserAcceptedTermnsOfUseChangeProposal(attributes)).build());
  }
}
