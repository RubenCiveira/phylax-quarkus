package net.civeira.phylax.features.access.useraccesstemporalcode.transition.resetpasswordrecover;

import java.util.ArrayList;
import java.util.List;
import java.util.function.Function;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.enterprise.inject.Instance;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.features.access.useraccesstemporalcode.UserAccessTemporalCode;
import net.civeira.phylax.features.access.useraccesstemporalcode.transition.UserAccessTemporalCodeChangeProposal;

@ApplicationScoped
@RequiredArgsConstructor
public class UserAccessTemporalCodeResetPasswordRecoverPipeline {

  /**
   * @autogenerated UpdateEntityRuleGenerator
   */
  private final Instance<UserAccessTemporalCodeResetPasswordRecoverStep> steps;

  /**
   * @autogenerated UpdateEntityRuleGenerator
   * @param original
   * @param last
   * @return
   */
  public UserAccessTemporalCode resetPasswordRecover(final UserAccessTemporalCode original,
      final Function<UserAccessTemporalCodeChangeProposal, UserAccessTemporalCode> last) {
    Function<UserAccessTemporalCodeResetPasswordRecoverTransition, UserAccessTemporalCode> finalHandler =
        t -> {
          if (t.hasErrors()) {
            throw t.asConstraintException();
          }
          return last.apply(t.getData());
        };
    List<UserAccessTemporalCodeResetPasswordRecoverStep> reversed =
        new ArrayList<>(steps.stream().toList()).reversed();
    Function<UserAccessTemporalCodeResetPasswordRecoverTransition, UserAccessTemporalCode> pipeline =
        reversed.stream().reduce(finalHandler,
            (next, step) -> transition -> step.resetPasswordRecover(transition, next),
            (f1, f2) -> f1);
    return pipeline.apply(UserAccessTemporalCodeResetPasswordRecoverTransition.builder()
        .data(new UserAccessTemporalCodeChangeProposal(original)).original(original).build());
  }
}
