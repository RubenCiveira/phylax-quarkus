/* @autogenerated */
package net.civeira.phylax.features.access.oauth.infrastructure.driver.impl.user;

import java.util.Locale;
import java.util.Optional;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.transaction.Transactional;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.features.access.oauth.application.usecase.PendingConsentUsecase;
import net.civeira.phylax.features.oauth.authentication.application.spi.UserConsentSpi;
import net.civeira.phylax.features.oauth.authentication.domain.model.AuthRequest;

@Transactional
@ApplicationScoped
@RequiredArgsConstructor
public class UserConsentIntector implements UserConsentSpi {
  private final PendingConsentUsecase pending;

  @Override
  public Optional<Consent> userRequiredConsent(Locale locale, AuthRequest request,
      String username) {
    return pending.userRequiredConsent(request, locale, username);
  }

  @Override
  public boolean confirmConsent(AuthRequest request, String username, String version) {
    return pending.confirmConsent(request, username, version);
  }
}
