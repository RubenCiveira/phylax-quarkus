package net.civeira.phylax.features.access.trustedclient.application.policy;

import jakarta.enterprise.event.Observes;
import net.civeira.phylax.common.security.Actor;
import net.civeira.phylax.features.access.trustedclient.application.usecase.enable.TrustedClientEnableAuthorizationDecision;

public class EnableOnlyForRoot {

  /**
   * Constant for admin role
   *
   * @autogenerated ConditionPolicyGenerator
   */
  private static final String ROL_ADMIN = "admin";

  /**
   * Constant for platformGlobalAccess scope
   *
   * @autogenerated ConditionPolicyGenerator
   */
  private static final String SCOPE_PLATFORM_GLOBAL_ACCESS = "platform:global_access";

  /**
   * @autogenerated ConditionPolicyGenerator
   * @param allowDecision
   */
  public void checkAllow(@Observes TrustedClientEnableAuthorizationDecision allowDecision) {
    Actor actor = allowDecision.getActor();
    if (!(actor.hasScope(SCOPE_PLATFORM_GLOBAL_ACCESS) && actor.hasRole(ROL_ADMIN))) {
      allowDecision.deny("Enable only for root");
    }
  }
}
