package net.civeira.phylax.features.access.useridentity.application.policy;

import jakarta.enterprise.event.Observes;
import net.civeira.phylax.common.security.Actor;
import net.civeira.phylax.features.access.useridentity.application.visibility.UserIdentityVisibilityCheck;

public class TenantAccesible {

  /**
   * Constant for root field
   *
   * @autogenerated VisibilityPolicyGenerator
   */
  private static final String CLAIM_ROOT = "root";

  /**
   * @autogenerated VisibilityPolicyGenerator
   * @param filter
   */
  public void filterVisibles(@Observes UserIdentityVisibilityCheck filter) {
    Actor actor = filter.getInteraction().getActor();
    if (!"true".equals(actor.getClaim(CLAIM_ROOT))) {
      filter.peek(fl -> fl.setUserTenantTenantAccesible(actor.getTenant().orElse(null)));
    }
  }
}
