package net.civeira.phylax.features.access.user.application.policy;

import jakarta.enterprise.event.Observes;
import net.civeira.phylax.common.security.Actor;
import net.civeira.phylax.features.access.user.application.visibility.UserFixedPropertiesProposal;

public class FixTenantExcludingRoot {

  /**
   * @autogenerated VisibilityPolicyGenerator
   * @param fixed
   */
  public void fixedFields(@Observes UserFixedPropertiesProposal fixed) {
    Actor actor = fixed.getActor();
    if (!(actor.hasScope("platform:global_access"))) {
      fixed.add("tenant");
    }
  }
}
