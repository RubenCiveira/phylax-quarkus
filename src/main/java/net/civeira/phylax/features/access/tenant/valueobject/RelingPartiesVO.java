package net.civeira.phylax.features.access.tenant.valueobject;

import java.util.List;

import lombok.EqualsAndHashCode;
import lombok.Getter;
import lombok.RequiredArgsConstructor;
import lombok.With;
import net.civeira.phylax.common.exception.ConstraintException;
import net.civeira.phylax.common.value.validation.AbstractFailList;
import net.civeira.phylax.common.value.validation.ConstraintFail;
import net.civeira.phylax.common.value.validation.ConstraintFailList;
import net.civeira.phylax.features.access.tenant.RelingParties;

/**
 * Value Object representing a list of relying parties (services or applications) associated with
 * this tenant. of tenant.
 * <p>
 * This class ensures that the reling parties is a non-null {@code String} and encapsulates its
 * validation and formatting logic. It is immutable and compliant with DDD (Domain-Driven Design)
 * principles.
 * </p>
 * <p>
 * Use {@link #from(String)} or {@link #tryFrom(Object)} to safely create instances. Validation
 * errors are reported via {@link ConstraintFailList} or thrown as {@link ConstraintException}.
 * </p>
 */
@Getter
@RequiredArgsConstructor
@With
@EqualsAndHashCode(onlyExplicitlyIncluded = true)
public class RelingPartiesVO {

  /**
   * An empty instance
   *
   * @autogenerated ValueObjectGenerator
   * @param relingParties
   * @return An empty instance
   */
  public static RelingPartiesVO from(final List<RelingParties> relingParties) {
    return tryFrom(relingParties);
  }

  /**
   * An empty instance
   *
   * @autogenerated ValueObjectGenerator
   * @param relingParties
   * @return An empty instance
   */
  public static RelingPartiesVO tryFrom(final Object relingParties) {
    ConstraintFailList list = new ConstraintFailList();
    RelingPartiesVO result = tryFrom(relingParties, list);
    if (list.hasErrors()) {
      throw new ConstraintException(list);
    }
    return result;
  }

  /**
   * An empty instance
   *
   * @autogenerated ValueObjectGenerator
   * @param relingParties temptative value
   * @param fails Error list
   * @return An empty instance
   */
  @SuppressWarnings("unchecked")
  public static <T extends AbstractFailList> RelingPartiesVO tryFrom(final Object relingParties,
      final T fails) {
    if (null == relingParties) {
      return new RelingPartiesVO(List.of());
    } else if (relingParties instanceof List) {
      return new RelingPartiesVO((List<RelingParties>) relingParties);
    } else {
      fails.add(new ConstraintFail("wrong-type", "relingParties", relingParties.getClass(),
          "A List<RelingParties> type is expected for relingParties"));
      return null;
    }
  }

  /**
   * The actual value of the name. * It is guaranteed to be a non-null String after successful
   * value.validation.
   *
   * @autogenerated ValueObjectGenerator
   */
  private final List<RelingParties> value;

  /**
   * the string value wrapped in square brackets.
   *
   * @autogenerated ValueObjectGenerator
   * @return the string value wrapped in square brackets.
   */
  @Override
  public String toString() {
    return "[" + value + "]";
  }
}
