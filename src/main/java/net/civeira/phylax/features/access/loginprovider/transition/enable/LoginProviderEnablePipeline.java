package net.civeira.phylax.features.access.loginprovider.transition.enable;

import java.util.ArrayList;
import java.util.List;
import java.util.function.Function;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.enterprise.inject.Instance;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.features.access.loginprovider.LoginProvider;
import net.civeira.phylax.features.access.loginprovider.transition.LoginProviderChangeProposal;

@ApplicationScoped
@RequiredArgsConstructor
public class LoginProviderEnablePipeline {

  /**
   * @autogenerated UpdateEntityRuleGenerator
   */
  private final Instance<LoginProviderEnableStep> steps;

  /**
   * @autogenerated UpdateEntityRuleGenerator
   * @param original
   * @param last
   * @return
   */
  public LoginProvider enable(final LoginProvider original,
      final Function<LoginProviderChangeProposal, LoginProvider> last) {
    Function<LoginProviderEnableTransition, LoginProvider> finalHandler = t -> {
      if (t.hasErrors()) {
        throw t.asConstraintException();
      }
      return last.apply(t.getData());
    };
    List<LoginProviderEnableStep> reversed = new ArrayList<>(steps.stream().toList()).reversed();
    Function<LoginProviderEnableTransition, LoginProvider> pipeline = reversed.stream().reduce(
        finalHandler, (next, step) -> transition -> step.enable(transition, next), (f1, f2) -> f1);
    return pipeline.apply(LoginProviderEnableTransition.builder()
        .data(new LoginProviderChangeProposal(original)).original(original).build());
  }
}
