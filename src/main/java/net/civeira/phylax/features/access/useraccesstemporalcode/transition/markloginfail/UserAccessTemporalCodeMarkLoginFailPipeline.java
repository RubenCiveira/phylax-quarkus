package net.civeira.phylax.features.access.useraccesstemporalcode.transition.markloginfail;

import java.util.ArrayList;
import java.util.List;
import java.util.function.Function;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.enterprise.inject.Instance;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.features.access.useraccesstemporalcode.UserAccessTemporalCode;
import net.civeira.phylax.features.access.useraccesstemporalcode.transition.UserAccessTemporalCodeChangeProposal;

@ApplicationScoped
@RequiredArgsConstructor
public class UserAccessTemporalCodeMarkLoginFailPipeline {

  /**
   * @autogenerated UpdateEntityRuleGenerator
   */
  private final Instance<UserAccessTemporalCodeMarkLoginFailStep> steps;

  /**
   * @autogenerated UpdateEntityRuleGenerator
   * @param original
   * @param last
   * @return
   */
  public UserAccessTemporalCode markLoginFail(final UserAccessTemporalCode original,
      final Function<UserAccessTemporalCodeChangeProposal, UserAccessTemporalCode> last) {
    Function<UserAccessTemporalCodeMarkLoginFailTransition, UserAccessTemporalCode> finalHandler =
        t -> {
          if (t.hasErrors()) {
            throw t.asConstraintException();
          }
          return last.apply(t.getData());
        };
    List<UserAccessTemporalCodeMarkLoginFailStep> reversed =
        new ArrayList<>(steps.stream().toList()).reversed();
    Function<UserAccessTemporalCodeMarkLoginFailTransition, UserAccessTemporalCode> pipeline =
        reversed.stream().reduce(finalHandler,
            (next, step) -> transition -> step.markLoginFail(transition, next), (f1, f2) -> f1);
    return pipeline.apply(UserAccessTemporalCodeMarkLoginFailTransition.builder()
        .data(new UserAccessTemporalCodeChangeProposal(original)).original(original).build());
  }
}
