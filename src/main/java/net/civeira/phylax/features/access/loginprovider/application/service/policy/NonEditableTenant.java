package net.civeira.phylax.features.access.loginprovider.application.service.policy;

import jakarta.enterprise.event.Observes;
import net.civeira.phylax.common.security.Actor;
import net.civeira.phylax.features.access.loginprovider.application.service.visibility.LoginProviderFixedFieldsProposal;

public class NonEditableTenant {

  /**
   * Constant for root field
   *
   * @autogenerated VisibilityPolicyGenerator
   */
  private static final String CLAIM_ROOT = "root";

  /**
   * @autogenerated VisibilityPolicyGenerator
   * @param fixed
   */
  public void fixedFields(@Observes LoginProviderFixedFieldsProposal fixed) {
    Actor actor = fixed.getQuery().getActor();
    if (!"true".equals(actor.getClaim(CLAIM_ROOT))) {
      fixed.add("tenant");
    }
  }
}
