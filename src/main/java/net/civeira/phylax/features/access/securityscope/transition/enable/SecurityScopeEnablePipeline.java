package net.civeira.phylax.features.access.securityscope.transition.enable;

import java.util.ArrayList;
import java.util.List;
import java.util.function.Function;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.enterprise.inject.Instance;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.features.access.securityscope.SecurityScope;
import net.civeira.phylax.features.access.securityscope.transition.SecurityScopeChangeProposal;

@ApplicationScoped
@RequiredArgsConstructor
public class SecurityScopeEnablePipeline {

  /**
   * @autogenerated UpdateEntityRuleGenerator
   */
  private final Instance<SecurityScopeEnableStep> steps;

  /**
   * @autogenerated UpdateEntityRuleGenerator
   * @param original
   * @param last
   * @return
   */
  public SecurityScope enable(final SecurityScope original,
      final Function<SecurityScopeChangeProposal, SecurityScope> last) {
    Function<SecurityScopeEnableTransition, SecurityScope> finalHandler = t -> {
      if (t.hasErrors()) {
        throw t.asConstraintException();
      }
      return last.apply(t.getData());
    };
    List<SecurityScopeEnableStep> reversed = new ArrayList<>(steps.stream().toList()).reversed();
    Function<SecurityScopeEnableTransition, SecurityScope> pipeline = reversed.stream().reduce(
        finalHandler, (next, step) -> transition -> step.enable(transition, next), (f1, f2) -> f1);
    return pipeline.apply(SecurityScopeEnableTransition.builder()
        .data(new SecurityScopeChangeProposal(original)).original(original).build());
  }
}
