package net.civeira.phylax.features.access.scopeassignation.transition.update;

import java.util.ArrayList;
import java.util.List;
import java.util.function.Function;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.enterprise.inject.Instance;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.features.access.scopeassignation.ScopeAssignation;
import net.civeira.phylax.features.access.scopeassignation.command.ScopeAssignationWriteAttributes;
import net.civeira.phylax.features.access.scopeassignation.transition.ScopeAssignationChangeProposal;

@ApplicationScoped
@RequiredArgsConstructor
public class ScopeAssignationUpdatePipeline {

  /**
   * @autogenerated UpdateEntityRuleGenerator
   */
  private final Instance<ScopeAssignationUpdateStep> steps;

  /**
   * @autogenerated UpdateEntityRuleGenerator
   * @param attributes
   * @param original
   * @param last
   * @return
   */
  public ScopeAssignation update(final ScopeAssignationWriteAttributes attributes,
      final ScopeAssignation original,
      final Function<ScopeAssignationChangeProposal, ScopeAssignation> last) {
    Function<ScopeAssignationUpdateTransition, ScopeAssignation> finalHandler = t -> {
      if (t.hasErrors()) {
        throw t.asConstraintException();
      }
      return last.apply(t.getData());
    };
    List<ScopeAssignationUpdateStep> reversed = new ArrayList<>(steps.stream().toList()).reversed();
    Function<ScopeAssignationUpdateTransition, ScopeAssignation> pipeline =
        reversed.stream().reduce(finalHandler,
            (next, step) -> transition -> step.update(transition, next), (f1, f2) -> f1);
    return pipeline.apply(ScopeAssignationUpdateTransition.builder()
        .data(new ScopeAssignationChangeProposal(attributes)).original(original).build());
  }
}
