package net.civeira.phylax.features.access.trustedclient.transition.create;

import java.util.ArrayList;
import java.util.List;
import java.util.function.Function;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.enterprise.inject.Instance;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.features.access.trustedclient.TrustedClient;
import net.civeira.phylax.features.access.trustedclient.command.TrustedClientWriteAttributes;
import net.civeira.phylax.features.access.trustedclient.transition.TrustedClientChangeProposal;

@ApplicationScoped
@RequiredArgsConstructor
public class TrustedClientCreatePipeline {

  /**
   * @autogenerated UpdateEntityRuleGenerator
   */
  private final Instance<TrustedClientCreateStep> steps;

  /**
   * @autogenerated UpdateEntityRuleGenerator
   * @param attributes
   * @param last
   * @return
   */
  public TrustedClient create(final TrustedClientWriteAttributes attributes,
      final Function<TrustedClientChangeProposal, TrustedClient> last) {
    Function<TrustedClientCreateTransition, TrustedClient> finalHandler = t -> {
      if (t.hasErrors()) {
        throw t.asConstraintException();
      }
      return last.apply(t.getData());
    };
    List<TrustedClientCreateStep> reversed = new ArrayList<>(steps.stream().toList()).reversed();
    Function<TrustedClientCreateTransition, TrustedClient> pipeline = reversed.stream().reduce(
        finalHandler, (next, step) -> transition -> step.create(transition, next), (f1, f2) -> f1);
    return pipeline.apply(TrustedClientCreateTransition.builder()
        .data(new TrustedClientChangeProposal(attributes)).build());
  }
}
