package net.civeira.phylax.features.notification.smtpoutboundconfig.domain.valueobject;

import java.util.Map;

import lombok.EqualsAndHashCode;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.common.exception.ConstraintException;
import net.civeira.phylax.common.value.importation.NumericLoad;
import net.civeira.phylax.common.value.validation.AbstractFailList;
import net.civeira.phylax.common.value.validation.ConstraintFail;
import net.civeira.phylax.common.value.validation.ConstraintFailList;

/**
 * Value Object representing el retry delay de smtp outbound config of smtpOutboundConfig.
 * <p>
 * This class ensures that the retry delay is a non-null {@code String} and encapsulates its
 * validation and formatting logic. It is immutable and compliant with DDD (Domain-Driven Design)
 * principles.
 * </p>
 * <p>
 * Use {@link #from(String)} or {@link #tryFrom(Object)} to safely create instances. Validation
 * errors are reported via {@link ConstraintFailList} or thrown as {@link ConstraintException}.
 * </p>
 */
@RequiredArgsConstructor
@EqualsAndHashCode
public class RetryDelayVO {

  /**
   * Crear an empty instance with no value
   *
   * @autogenerated ValueObjectGenerator
   * @param retryDelay
   * @return An empty instance
   */
  public static RetryDelayVO from(final Integer retryDelay) {
    return tryFrom(retryDelay);
  }

  /**
   * Crear an empty instance with no value
   *
   * @autogenerated ValueObjectGenerator
   * @param retryDelay
   * @return An empty instance
   */
  public static RetryDelayVO tryFrom(final Object retryDelay) {
    ConstraintFailList list = new ConstraintFailList();
    RetryDelayVO result = tryFrom(retryDelay, list);
    if (list.hasErrors()) {
      throw new ConstraintException("Invalid value for RetryDelayVO.retry-delay", list);
    }
    return result;
  }

  /**
   * Crear an empty instance with no value
   *
   * @autogenerated ValueObjectGenerator
   * @param retryDelay temptative value
   * @param fails Error list
   * @return An empty instance
   */
  public static <T extends AbstractFailList> RetryDelayVO tryFrom(final Object retryDelay,
      final T fails) {
    if (null == retryDelay) {
      fails.add(new ConstraintFail("not-null", "retryDelay", null, "Cant be null"));
      return null;
    } else if (retryDelay instanceof Integer castedRetryDelay) {
      return new RetryDelayVO(castedRetryDelay);
    } else {
      fails.add(new ConstraintFail("wrong-type", "retryDelay", retryDelay.getClass(),
          "A Integer type is expected for retryDelay"));
      return null;
    }
  }

  /**
   * Crear an empty instance with no value
   *
   * @autogenerated ValueObjectGenerator
   * @param retryDelay temptative value
   * @param fails Error list
   * @return An empty instance
   */
  public static <T extends AbstractFailList> RetryDelayVO tryFromString(final String retryDelay,
      final T fails) {
    return NumericLoad.toInt("retryDelay", retryDelay, fails).map(value -> tryFrom(value, fails))
        .orElse(null);
  }

  /**
   * The actual value of the name. * It is guaranteed to be a non-null String after successful
   * value.validation.
   *
   * @autogenerated ValueObjectGenerator
   */
  private final Integer value;

  /**
   * Get the vo value
   *
   * @autogenerated ValueObjectGenerator
   * @return
   */
  public Integer getRetryDelay() {
    return value;
  }

  /**
   * @autogenerated ValueObjectGenerator
   * @param map
   */
  public void retryDelayToMap(final Map<String, String> map) {
    if (null != value) {
      map.put("retryDelay", String.valueOf(value));
    }
  }

  /**
   * Returns a formatted string representation of the name.
   *
   * @autogenerated ValueObjectGenerator
   * @return the string value wrapped in square brackets.
   */
  @Override
  public String toString() {
    return "[" + value + "]";
  }
}
