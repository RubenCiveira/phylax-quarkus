package net.civeira.phylax.features.notification.smtpoutboundconfig.application.importation;

import java.util.Map;
import java.util.Optional;

import lombok.RequiredArgsConstructor;
import net.civeira.phylax.common.value.validation.ConstraintFail;
import net.civeira.phylax.common.value.validation.ConstraintFailList;
import net.civeira.phylax.features.access.tenant.domain.Tenant;
import net.civeira.phylax.features.access.tenant.domain.TenantChangeSet;
import net.civeira.phylax.features.access.tenant.domain.gateway.TenantFilter;
import net.civeira.phylax.features.access.tenant.domain.gateway.TenantReadRepositoryGateway;
import net.civeira.phylax.features.access.tenant.domain.gateway.TenantWriteRepositoryGateway;
import net.civeira.phylax.features.notification.smtpoutboundconfig.domain.SmtpOutboundConfig;
import net.civeira.phylax.features.notification.smtpoutboundconfig.domain.SmtpOutboundConfigChangeSet;
import net.civeira.phylax.features.notification.smtpoutboundconfig.domain.gateway.SmtpOutboundConfigFilter;
import net.civeira.phylax.features.notification.smtpoutboundconfig.domain.gateway.SmtpOutboundConfigWriteRepositoryGateway;
import net.civeira.phylax.features.notification.smtpoutboundconfig.domain.valueobject.MaxRetriesVO;
import net.civeira.phylax.features.notification.smtpoutboundconfig.domain.valueobject.PortVO;
import net.civeira.phylax.features.notification.smtpoutboundconfig.domain.valueobject.RateLimitVO;
import net.civeira.phylax.features.notification.smtpoutboundconfig.domain.valueobject.RetryDelayVO;
import net.civeira.phylax.features.notification.smtpoutboundconfig.domain.valueobject.TimeoutVO;
import net.civeira.phylax.features.notification.smtpoutboundconfig.domain.valueobject.UseTlsVO;

@RequiredArgsConstructor
public class SmtpOutboundConfigImportationService {

  /**
   * @autogenerated VisibilityFilterGenerator
   */
  private final SmtpOutboundConfigWriteRepositoryGateway repository;

  /**
   * @autogenerated VisibilityFilterGenerator
   */
  private final TenantReadRepositoryGateway tenantReadRepository;

  /**
   * @autogenerated VisibilityFilterGenerator
   */
  private final TenantWriteRepositoryGateway tenantWriteRepository;

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param row
   * @return
   */
  public SmtpOutboundConfigImportationResult load(final Map<String, String> row) {
    return load(row, SmtpOutboundConfigImportationRelatedResolver.builder().build());
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param row
   * @param resolver
   * @return
   */
  public SmtpOutboundConfigImportationResult load(final Map<String, String> row,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    ConstraintFailList fails = new ConstraintFailList();
    String uid = row.get("uid");
    if (null == uid) {
      fails.add(new ConstraintFail("NOT_NULL", "uid", null));
    }
    boolean exists = false;
    SmtpOutboundConfig entity = null;
    if (!fails.isEmpty()) {
      Optional<SmtpOutboundConfig> existing =
          repository.findForUpdate(SmtpOutboundConfigFilter.builder().uid(uid).build());
      SmtpOutboundConfigChangeSet change = toChangeSet(row, fails, resolver);
      if (existing.isPresent()) {
        entity = existing.get().update(change);
        exists = true;
      } else {
        entity = SmtpOutboundConfig.create(change.newUid());
      }
    }
    return fails.isEmpty() ? new SmtpOutboundConfigImportationResult(entity, exists)
        : new SmtpOutboundConfigImportationResult(fails);
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  public SmtpOutboundConfigChangeSet toChangeSet(final Map<String, String> row,
      final ConstraintFailList fails, final SmtpOutboundConfigImportationRelatedResolver resolver) {
    SmtpOutboundConfigChangeSet change = new SmtpOutboundConfigChangeSet();
    change = readTenant(change, row, fails, resolver);
    change = readHost(change, row, fails, resolver);
    change = readPort(change, row, fails, resolver);
    change = readLogin(change, row, fails, resolver);
    change = readPassword(change, row, fails, resolver);
    change = readSenderName(change, row, fails, resolver);
    change = readSenderEmail(change, row, fails, resolver);
    change = readTimeout(change, row, fails, resolver);
    change = readUseTls(change, row, fails, resolver);
    change = readMaxRetries(change, row, fails, resolver);
    change = readRetryDelay(change, row, fails, resolver);
    change = readRateLimit(change, row, fails, resolver);
    return change;
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param change
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  private SmtpOutboundConfigChangeSet readHost(final SmtpOutboundConfigChangeSet change,
      final Map<String, String> row, final ConstraintFailList fails,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    String host = row.get("host");
    if (host != null) {
      return change.host(host);
    } else {
      return change.unsetHost();
    }
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param change
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  private SmtpOutboundConfigChangeSet readLogin(final SmtpOutboundConfigChangeSet change,
      final Map<String, String> row, final ConstraintFailList fails,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    String login = row.get("login");
    if (login != null) {
      return change.login(login);
    } else {
      return change.unsetLogin();
    }
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param change
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  private SmtpOutboundConfigChangeSet readMaxRetries(final SmtpOutboundConfigChangeSet change,
      final Map<String, String> row, final ConstraintFailList fails,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    String maxRetries = row.get("maxRetries");
    if (maxRetries != null) {
      return change.maxRetries(MaxRetriesVO.tryFromString(maxRetries, fails));
    } else {
      return change.unsetMaxRetries();
    }
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param change
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  private SmtpOutboundConfigChangeSet readPassword(final SmtpOutboundConfigChangeSet change,
      final Map<String, String> row, final ConstraintFailList fails,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    String password = row.get("password");
    if (password != null) {
      return change.passwordPlain(password);
    } else {
      return change.unsetPassword();
    }
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param change
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  private SmtpOutboundConfigChangeSet readPort(final SmtpOutboundConfigChangeSet change,
      final Map<String, String> row, final ConstraintFailList fails,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    String port = row.get("port");
    if (port != null) {
      return change.port(PortVO.tryFromString(port, fails));
    } else {
      return change.unsetPort();
    }
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param change
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  private SmtpOutboundConfigChangeSet readRateLimit(final SmtpOutboundConfigChangeSet change,
      final Map<String, String> row, final ConstraintFailList fails,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    String rateLimit = row.get("rateLimit");
    if (rateLimit != null) {
      return change.rateLimit(RateLimitVO.tryFromString(rateLimit, fails));
    } else {
      return change.unsetRateLimit();
    }
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param change
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  private SmtpOutboundConfigChangeSet readRetryDelay(final SmtpOutboundConfigChangeSet change,
      final Map<String, String> row, final ConstraintFailList fails,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    String retryDelay = row.get("retryDelay");
    if (retryDelay != null) {
      return change.retryDelay(RetryDelayVO.tryFromString(retryDelay, fails));
    } else {
      return change.unsetRetryDelay();
    }
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param change
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  private SmtpOutboundConfigChangeSet readSenderEmail(final SmtpOutboundConfigChangeSet change,
      final Map<String, String> row, final ConstraintFailList fails,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    String senderEmail = row.get("senderEmail");
    if (senderEmail != null) {
      return change.senderEmail(senderEmail);
    } else {
      return change.unsetSenderEmail();
    }
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param change
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  private SmtpOutboundConfigChangeSet readSenderName(final SmtpOutboundConfigChangeSet change,
      final Map<String, String> row, final ConstraintFailList fails,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    String senderName = row.get("senderName");
    if (senderName != null) {
      return change.senderName(senderName);
    } else {
      return change.unsetSenderName();
    }
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param change
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  private SmtpOutboundConfigChangeSet readTenant(final SmtpOutboundConfigChangeSet change,
      final Map<String, String> row, final ConstraintFailList fails,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    String tenant = row.get("tenant");
    if (tenant != null) {
      Optional<Tenant> existing =
          tenantReadRepository.find(TenantFilter.builder().name(tenant).build());
      if (existing.isPresent()) {
        return change.tenant(existing.get());
      } else if (resolver.isCreateMissingTenant()) {
        TenantChangeSet childChanges = new TenantChangeSet().newUid();
        Tenant entity = (null == resolver.getMissingTenantFactory()) ? Tenant.create(childChanges)
            : resolver.getMissingTenantFactory().apply(childChanges);
        return change.tenant(tenantWriteRepository.create(entity));
      } else {
        fails.add(new ConstraintFail("NOT_FOUND", "tenant", tenant));
        return change;
      }
    } else {
      return change.unsetTenant();
    }
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param change
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  private SmtpOutboundConfigChangeSet readTimeout(final SmtpOutboundConfigChangeSet change,
      final Map<String, String> row, final ConstraintFailList fails,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    String timeout = row.get("timeout");
    if (timeout != null) {
      return change.timeout(TimeoutVO.tryFromString(timeout, fails));
    } else {
      return change.unsetTimeout();
    }
  }

  /**
   * @autogenerated VisibilityFilterGenerator
   * @param change
   * @param row
   * @param fails
   * @param resolver
   * @return
   */
  private SmtpOutboundConfigChangeSet readUseTls(final SmtpOutboundConfigChangeSet change,
      final Map<String, String> row, final ConstraintFailList fails,
      final SmtpOutboundConfigImportationRelatedResolver resolver) {
    String useTls = row.get("useTls");
    if (useTls != null) {
      return change.useTls(UseTlsVO.tryFromString(useTls, fails));
    } else {
      return change.unsetUseTls();
    }
  }
}
