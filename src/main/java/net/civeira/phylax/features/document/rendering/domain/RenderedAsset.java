/* @autogenerated */
package net.civeira.phylax.features.document.rendering.domain;

import lombok.Builder;
import lombok.Getter;
import lombok.RequiredArgsConstructor;

/**
 * An asset (e.g. a stylesheet) that was associated with the rendered document.
 *
 * <p>
 * When the output format is {@link TemplateOutputFormat#HTML_EMBEDDED} the rendering engine inlines
 * text assets (CSS) directly into the HTML and marks them as {@code embedded = true}. For other
 * formats the asset is returned as-is so callers can link it externally.
 */
@Getter
@Builder
@RequiredArgsConstructor
public class RenderedAsset {

  /** Logical identifier for this asset, e.g. {@code "theme-css"}. */
  private final String code;

  /** MIME type of the asset content, e.g. {@code "text/css"}. */
  private final String contentType;

  /**
   * Raw text content of the asset (for text-based assets such as CSS). Binary assets (images,
   * fonts) are not represented here; they are served externally.
   */
  private final String content;

  /**
   * {@code true} when this asset has already been inlined into the rendered HTML (e.g. as a
   * {@code <style>} block). Callers can skip external linking for embedded assets.
   */
  private final boolean embedded;
}
