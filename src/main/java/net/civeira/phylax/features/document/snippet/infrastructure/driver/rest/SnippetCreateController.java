package net.civeira.phylax.features.document.snippet.infrastructure.driver.rest;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.transaction.Transactional;
import jakarta.ws.rs.core.Response;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.common.infrastructure.CurrentRequest;
import net.civeira.phylax.common.telemetry.ApiObserved;
import net.civeira.phylax.features.access.tenant.domain.TenantReference;
import net.civeira.phylax.features.document.snippet.application.usecase.create.SnippetCreateInput;
import net.civeira.phylax.features.document.snippet.application.usecase.create.SnippetCreateProjection;
import net.civeira.phylax.features.document.snippet.application.usecase.create.SnippetCreateUsecase;
import net.civeira.phylax.generated.openapi.model.SnippetApiDto;
import net.civeira.phylax.generated.openapi.model.TenantApiRef;

@RequiredArgsConstructor
@ApplicationScoped
public class SnippetCreateController {

  /**
   * @autogenerated CreateControllerGenerator
   */
  private final SnippetCreateUsecase create;

  /**
   * @autogenerated CreateControllerGenerator
   */
  private final CurrentRequest currentRequest;

  /**
   * @autogenerated CreateControllerGenerator
   * @param snippet
   * @return
   */
  @Transactional
  @ApiObserved("Api to create an entity of snippet")
  public Response snippetApiCreate(SnippetApiDto snippet) {
    SnippetCreateProjection created =
        create.create(currentRequest.interaction(), toDomainModel(snippet));
    return Response.ok(toApiModel(created)).build();
  }

  /**
   * @autogenerated CreateControllerGenerator
   * @param dto
   * @return
   */
  private SnippetApiDto toApiModel(SnippetCreateProjection dto) {
    SnippetApiDto snippetApiDto = new SnippetApiDto();
    snippetApiDto.setUid(dto.getUid());
    snippetApiDto.setCode(dto.getCode());
    snippetApiDto.setTenant(new TenantApiRef().$ref(dto.getTenantReference()));
    snippetApiDto.setEnabled(dto.getEnabled());
    snippetApiDto.setVersion(dto.getVersion());
    return snippetApiDto;
  }

  /**
   * @autogenerated CreateControllerGenerator
   * @param snippetApiDto
   * @return
   */
  private SnippetCreateInput toDomainModel(SnippetApiDto snippetApiDto) {
    SnippetCreateInput dto = new SnippetCreateInput();
    if (null != snippetApiDto.getUid()) {
      dto.setUid(snippetApiDto.getUid());
    }
    if (null != snippetApiDto.getCode()) {
      dto.setCode(snippetApiDto.getCode());
    }
    if (null != snippetApiDto.getTenant()) {
      dto.setTenant(TenantReference.of(snippetApiDto.getTenant().get$Ref()));
    }
    if (null != snippetApiDto.getEnabled()) {
      dto.setEnabled(snippetApiDto.getEnabled());
    }
    if (null != snippetApiDto.getVersion()) {
      dto.setVersion(snippetApiDto.getVersion());
    }
    return dto;
  }
}
