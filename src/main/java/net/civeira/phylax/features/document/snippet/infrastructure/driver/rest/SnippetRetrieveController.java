package net.civeira.phylax.features.document.snippet.infrastructure.driver.rest;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.ws.rs.core.Response;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.common.infrastructure.CurrentRequest;
import net.civeira.phylax.common.telemetry.ApiObserved;
import net.civeira.phylax.common.telemetry.Trace;
import net.civeira.phylax.features.document.snippet.application.usecase.retrieve.SnippetRetrieveProjection;
import net.civeira.phylax.features.document.snippet.application.usecase.retrieve.SnippetRetrieveUsecase;
import net.civeira.phylax.features.document.snippet.domain.SnippetReference;
import net.civeira.phylax.generated.openapi.model.SnippetApiDto;
import net.civeira.phylax.generated.openapi.model.TenantApiRef;

@RequiredArgsConstructor
@ApplicationScoped
public class SnippetRetrieveController {

  /**
   * @autogenerated RetrieveControllerGenerator
   */
  private final CurrentRequest currentRequest;

  /**
   * @autogenerated RetrieveControllerGenerator
   */
  private final SnippetRetrieveUsecase retrieve;

  /**
   * @autogenerated RetrieveControllerGenerator
   * @param uid
   * @return
   */
  @ApiObserved("Api to retrieve an entity of snippet")
  public Response snippetApiRetrieve(final String uid) {
    Trace.setAttribute("ref", uid);
    SnippetRetrieveProjection retrieved =
        retrieve.retrieve(currentRequest.interaction(), SnippetReference.of(uid));
    /* .header("Last-Modified", value.getSince().format(DateTimeFormatter.RFC_1123_DATE_TIME))) */
    return Response.ok(toApiModel(retrieved)).build();
  }

  /**
   * @autogenerated RetrieveControllerGenerator
   * @param dto
   * @return
   */
  private SnippetApiDto toApiModel(SnippetRetrieveProjection dto) {
    SnippetApiDto snippetApiDto = new SnippetApiDto();
    snippetApiDto.setUid(dto.getUid());
    snippetApiDto.setCode(dto.getCode());
    snippetApiDto.setTenant(new TenantApiRef().$ref(dto.getTenantReference()));
    snippetApiDto.setEnabled(dto.getEnabled());
    snippetApiDto.setVersion(dto.getVersion());
    return snippetApiDto;
  }
}
