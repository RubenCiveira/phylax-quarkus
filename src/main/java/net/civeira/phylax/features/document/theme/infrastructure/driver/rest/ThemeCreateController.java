package net.civeira.phylax.features.document.theme.infrastructure.driver.rest;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.transaction.Transactional;
import jakarta.ws.rs.core.Response;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.common.infrastructure.CurrentRequest;
import net.civeira.phylax.common.telemetry.ApiObserved;
import net.civeira.phylax.features.access.tenant.domain.TenantReference;
import net.civeira.phylax.features.document.theme.application.usecase.create.ThemeCreateInput;
import net.civeira.phylax.features.document.theme.application.usecase.create.ThemeCreateProjection;
import net.civeira.phylax.features.document.theme.application.usecase.create.ThemeCreateUsecase;
import net.civeira.phylax.generated.openapi.model.TenantApiRef;
import net.civeira.phylax.generated.openapi.model.ThemeApiDto;

@RequiredArgsConstructor
@ApplicationScoped
public class ThemeCreateController {

  /**
   * @autogenerated CreateControllerGenerator
   */
  private final ThemeCreateUsecase create;

  /**
   * @autogenerated CreateControllerGenerator
   */
  private final CurrentRequest currentRequest;

  /**
   * @autogenerated CreateControllerGenerator
   * @param theme
   * @return
   */
  @Transactional
  @ApiObserved("Api to create an entity of theme")
  public Response themeApiCreate(ThemeApiDto theme) {
    ThemeCreateProjection created =
        create.create(currentRequest.interaction(), toDomainModel(theme));
    return Response.ok(toApiModel(created)).build();
  }

  /**
   * @autogenerated CreateControllerGenerator
   * @param dto
   * @return
   */
  private ThemeApiDto toApiModel(ThemeCreateProjection dto) {
    ThemeApiDto themeApiDto = new ThemeApiDto();
    themeApiDto.setUid(dto.getUid());
    themeApiDto.setTenant(new TenantApiRef().$ref(dto.getTenantReference()));
    themeApiDto.setName(dto.getName());
    themeApiDto.setIsDefault(dto.getIsDefault());
    themeApiDto.setEnabled(dto.getEnabled());
    themeApiDto.setCustomCss(dto.getCustomCss());
    themeApiDto.setVersion(dto.getVersion());
    return themeApiDto;
  }

  /**
   * @autogenerated CreateControllerGenerator
   * @param themeApiDto
   * @return
   */
  private ThemeCreateInput toDomainModel(ThemeApiDto themeApiDto) {
    ThemeCreateInput dto = new ThemeCreateInput();
    if (null != themeApiDto.getUid()) {
      dto.setUid(themeApiDto.getUid());
    }
    if (null != themeApiDto.getTenant()) {
      dto.setTenant(TenantReference.of(themeApiDto.getTenant().get$Ref()));
    }
    if (null != themeApiDto.getName()) {
      dto.setName(themeApiDto.getName());
    }
    if (null != themeApiDto.getIsDefault()) {
      dto.setIsDefault(themeApiDto.getIsDefault());
    }
    if (null != themeApiDto.getEnabled()) {
      dto.setEnabled(themeApiDto.getEnabled());
    }
    if (null != themeApiDto.getCustomCss()) {
      dto.setCustomCss(themeApiDto.getCustomCss());
    }
    if (null != themeApiDto.getVersion()) {
      dto.setVersion(themeApiDto.getVersion());
    }
    return dto;
  }
}
