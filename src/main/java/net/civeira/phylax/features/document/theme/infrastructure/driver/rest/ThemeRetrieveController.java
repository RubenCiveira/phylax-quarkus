package net.civeira.phylax.features.document.theme.infrastructure.driver.rest;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.ws.rs.core.Response;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.common.infrastructure.CurrentRequest;
import net.civeira.phylax.common.telemetry.ApiObserved;
import net.civeira.phylax.common.telemetry.Trace;
import net.civeira.phylax.features.document.theme.application.usecase.retrieve.ThemeRetrieveProjection;
import net.civeira.phylax.features.document.theme.application.usecase.retrieve.ThemeRetrieveUsecase;
import net.civeira.phylax.features.document.theme.domain.ThemeReference;
import net.civeira.phylax.generated.openapi.model.TenantApiRef;
import net.civeira.phylax.generated.openapi.model.ThemeApiDto;

@RequiredArgsConstructor
@ApplicationScoped
public class ThemeRetrieveController {

  /**
   * @autogenerated RetrieveControllerGenerator
   */
  private final CurrentRequest currentRequest;

  /**
   * @autogenerated RetrieveControllerGenerator
   */
  private final ThemeRetrieveUsecase retrieve;

  /**
   * @autogenerated RetrieveControllerGenerator
   * @param uid
   * @return
   */
  @ApiObserved("Api to retrieve an entity of theme")
  public Response themeApiRetrieve(final String uid) {
    Trace.setAttribute("ref", uid);
    ThemeRetrieveProjection retrieved =
        retrieve.retrieve(currentRequest.interaction(), ThemeReference.of(uid));
    /* .header("Last-Modified", value.getSince().format(DateTimeFormatter.RFC_1123_DATE_TIME))) */
    return Response.ok(toApiModel(retrieved)).build();
  }

  /**
   * @autogenerated RetrieveControllerGenerator
   * @param dto
   * @return
   */
  private ThemeApiDto toApiModel(ThemeRetrieveProjection dto) {
    ThemeApiDto themeApiDto = new ThemeApiDto();
    themeApiDto.setUid(dto.getUid());
    themeApiDto.setTenant(new TenantApiRef().$ref(dto.getTenantReference()));
    themeApiDto.setName(dto.getName());
    themeApiDto.setIsDefault(dto.getIsDefault());
    themeApiDto.setEnabled(dto.getEnabled());
    themeApiDto.setCustomCss(dto.getCustomCss());
    themeApiDto.setVersion(dto.getVersion());
    return themeApiDto;
  }
}
