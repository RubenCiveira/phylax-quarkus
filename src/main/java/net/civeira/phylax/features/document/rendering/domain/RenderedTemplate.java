/* @autogenerated */
package net.civeira.phylax.features.document.rendering.domain;

import java.util.List;
import java.util.Optional;

import lombok.Builder;
import lombok.Getter;
import lombok.RequiredArgsConstructor;

/**
 * Result produced by the rendering engine: fully substituted subject, HTML body, optional
 * plain-text body, and associated assets.
 */
@Getter
@Builder
@RequiredArgsConstructor
public class RenderedTemplate {

  /** Email subject. Empty when the channel does not use it. */
  private final String subject;

  /** Rendered HTML body. Always present. */
  private final String htmlContent;

  /** Rendered plain-text body. Empty when the template version has no text content. */
  private final String textContent;

  /**
   * Assets associated with this rendered document (e.g. stylesheets). Assets with
   * {@link RenderedAsset#isEmbedded()} set to {@code true} are already inlined into
   * {@link #htmlContent} and do not need to be linked externally.
   */
  @Builder.Default
  private final List<RenderedAsset> assets = List.of();

  public Optional<String> getSubject() {
    return Optional.ofNullable(subject);
  }

  public Optional<String> getTextContent() {
    return Optional.ofNullable(textContent);
  }
}
