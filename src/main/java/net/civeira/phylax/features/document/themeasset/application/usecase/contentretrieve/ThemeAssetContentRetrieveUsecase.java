package net.civeira.phylax.features.document.themeasset.application.usecase.contentretrieve;

import jakarta.enterprise.context.ApplicationScoped;
import jakarta.enterprise.event.Event;
import lombok.RequiredArgsConstructor;
import net.civeira.phylax.common.exception.NotAllowedException;
import net.civeira.phylax.common.exception.NotFoundException;
import net.civeira.phylax.common.infrastructure.store.BinaryContent;
import net.civeira.phylax.common.security.OperationContext;
import net.civeira.phylax.common.security.Permission;
import net.civeira.phylax.common.telemetry.Observed;
import net.civeira.phylax.common.telemetry.Trace;
import net.civeira.phylax.features.document.themeasset.application.visibility.ThemeAssetsVisibility;
import net.civeira.phylax.features.document.themeasset.domain.ThemeAsset;
import net.civeira.phylax.features.document.themeasset.domain.ThemeAssetRef;
import net.civeira.phylax.features.document.themeasset.domain.gateway.ThemeAssetContentUploadGateway;

@ApplicationScoped
@RequiredArgsConstructor
public class ThemeAssetContentRetrieveUsecase {

  /**
   * @autogenerated ListUsecaseGenerator
   */
  private final Event<ThemeAssetContentRetrieveAuthorizationDecision> retrieveAllowEmitter;

  /**
   * @autogenerated ListUsecaseGenerator
   */
  private final ThemeAssetContentUploadGateway store;

  /**
   * @autogenerated ListUsecaseGenerator
   */
  private final ThemeAssetsVisibility visibility;

  /**
   * @autogenerated ListUsecaseGenerator
   * @param context
   * @param reference
   * @return
   */
  @Observed("Checking if is allowed to retrieve content an entity of theme asset")
  public Permission allow(final OperationContext context, final ThemeAssetRef reference) {
    Trace.setAttribute("ref", reference.getUid());
    Permission response = checkPermission(context, reference);
    Trace.setAttribute("allowed", response.isAllowed());
    return response;
  }

  /**
   * @autogenerated ListUsecaseGenerator
   * @param context
   * @return
   */
  @Observed("Checking if is allowed to retrieve content an entity of theme asset")
  public Permission allow(final OperationContext context) {
    Permission response = checkPermission(context);
    Trace.setAttribute("allowed", response.isAllowed());
    return response;
  }

  /**
   * @autogenerated ListUsecaseGenerator
   * @param context
   * @param reference
   * @return The slide with some values
   */
  @Observed("Updating an entity of theme asset")
  public BinaryContent read(final OperationContext context, final ThemeAssetRef reference) {
    Permission permission = checkPermission(context, reference);
    if (!permission.isAllowed()) {
      Trace.addEvent("The policies for theme asset dont allow to retrieve content: "
          + permission.getDescription());
      throw new NotAllowedException(permission.getDescription());
    }
    ThemeAsset entity = visibility.retrieveVisible(context, reference.getUid())
        .orElseThrow(() -> new NotFoundException(""));
    return store.readContent(entity).orElseThrow(() -> new NotFoundException(""));
  }

  /**
   * @autogenerated ListUsecaseGenerator
   * @param context
   * @param reference
   * @return
   */
  private Permission checkPermission(final OperationContext context,
      final ThemeAssetRef reference) {
    ThemeAssetContentRetrieveAuthorizationDecision proposal =
        new ThemeAssetContentRetrieveAuthorizationDecision(context, reference,
            Permission.allow("Allow to retrieve content by default"));
    retrieveAllowEmitter.fire(proposal);
    return proposal.getPermission();
  }

  /**
   * @autogenerated ListUsecaseGenerator
   * @param context
   * @return
   */
  private Permission checkPermission(final OperationContext context) {
    ThemeAssetContentRetrieveAuthorizationDecision proposal =
        new ThemeAssetContentRetrieveAuthorizationDecision(context,
            Permission.allow("Allow to retrieve content by default"));
    retrieveAllowEmitter.fire(proposal);
    return proposal.getPermission();
  }
}
