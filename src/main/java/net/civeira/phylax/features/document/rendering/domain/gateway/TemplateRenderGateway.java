/* @autogenerated */
package net.civeira.phylax.features.document.rendering.domain.gateway;

import net.civeira.phylax.features.document.rendering.domain.RenderedTemplate;
import net.civeira.phylax.features.document.rendering.domain.TemplateRenderRequest;

/**
 * Port for the template rendering engine.
 * <p>
 * The implementation is responsible solely for the technical substitution of variables and snippet
 * partials into raw template content strings. All domain logic (template lookup, version selection,
 * variable resolution) is handled by the use case before calling this gateway.
 * </p>
 */
public interface TemplateRenderGateway {

  /**
   * Render a template from pre-resolved raw content and variables.
   *
   * @param request Raw content strings, snippets map and merged variable map.
   * @return The rendered template with all placeholders substituted.
   */
  RenderedTemplate render(TemplateRenderRequest request);
}
