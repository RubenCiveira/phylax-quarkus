/* @autogenerated */
/**
 * OpenID Connect discovery document and provider metadata (RFC 8414 / OIDC Discovery 1.0).
 *
 * <p>This bounded context owns the OIDC provider metadata endpoint that allows relying parties
 * and OAuth clients to automatically discover the server's capabilities, endpoint URLs, supported
 * algorithms, and optional features without manual configuration. It is a thin, read-only context:
 * all values are derived from server configuration; there is no mutable state.
 *
 * <p><b>Key domain types:</b>
 * <ul>
 *   <li>{@code OpenIdConfiguration} — the primary domain object. A structured representation of
 *       the discovery document served at {@code /.well-known/openid-configuration}. Contains:
 *       the issuer URL; all standard endpoint URLs (authorization, token, userinfo, JWKS,
 *       introspection, revocation, device_authorization, pushed_authorization_request,
 *       end_session, check_session_iframe); the supported grant types (authorization_code,
 *       refresh_token, urn:ietf:params:oauth:grant-type:device_code, client_credentials);
 *       supported response types and modes; supported scopes; supported signing algorithms
 *       (id_token_signing_alg_values_supported, etc.); PKCE code-challenge methods; claims
 *       supported; token endpoint authentication methods; back-channel and front-channel logout
 *       support flags; and the optional {@code MtlsEndpointAliases} block for mTLS endpoints.</li>
 *   <li>{@code MtlsEndpointAliases} — nested value object within the discovery document that
 *       provides mTLS-variant URLs for the token and pushed-authorization endpoints as specified
 *       by RFC 8705 (OAuth 2.0 Mutual-TLS Client Authentication). Included in the discovery
 *       document only when mTLS client authentication is enabled on the server.</li>
 * </ul>
 *
 * <p><b>Infrastructure driver:</b>
 * <ul>
 *   <li>{@code OpenIdConfigurationController} — REST driver implementing the OIDC Discovery
 *       endpoint. Serves GET /.well-known/openid-configuration with an
 *       {@code application/json} response. The document is assembled from server configuration
 *       properties on application startup, cached, and served without any per-request database
 *       calls. Supports the {@code Access-Control-Allow-Origin: *} CORS header required by the
 *       OIDC Discovery specification so that browser-based relying parties can fetch it
 *       cross-origin from the authorization server's well-known URI.</li>
 * </ul>
 *
 * <p><b>Internal structure:</b>
 * <pre>
 * oidc/
 * ├── domain/
 * │   ├── OpenIdConfiguration.java      — OIDC discovery document model
 * │   └── MtlsEndpointAliases.java      — mTLS endpoint aliases VO
 * └── infrastructure/
 *     ├── driver/rest/
 *     │   └── OpenIdConfigurationController.java   — GET /.well-known/openid-configuration
 *     └── (no driven adapters — purely configuration-derived, no persistence)
 * </pre>
 *
 * <p><b>Dependencies:</b> No runtime dependency on other bounded contexts. Server configuration
 * properties (issuer, base URL) are injected via Quarkus {@code @ConfigProperty}. The list of
 * supported features reflects the capabilities of the Authentication, TokenSecurity, Device, PAR,
 * and WebAuthn contexts but is assembled statically at startup, not dynamically queried.
 *
 * <p><b>Stability:</b> very stable; the OIDC Discovery spec is finalized. New fields are added
 * only when new OAuth extensions (e.g., DPoP, JAR, RAR) are implemented in other contexts.
 * The only maintenance burden is keeping the discovery document in sync with implemented features.
 */
package net.civeira.phylax.features.oauth.oidc;
