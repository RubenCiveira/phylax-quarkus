/* @autogenerated */
/**
 * Tenant-aware HTML theming and two-pass page decoration for all OIDC browser flows.
 *
 * <p>This bounded context provides the rendering pipeline that wraps every HTML page produced by
 * the authorization server in the active tenant's visual theme. It implements a two-pass
 * template rendering model that separates inner page content (form, error messages, step-specific
 * markup) from the outer layout (navigation, header, footer, CSS variables, tenant branding)
 * without coupling any driver to theme resolution logic.
 *
 * <p><b>Key domain types:</b>
 * <ul>
 *   <li>{@code DecoratePageGateway} — the sole outbound port. Declares two operations used by
 *       {@code DecorateHtml}: {@code renderPage(templateName, model)} for the inner-content
 *       template pass (e.g., rendering the login form template as a self-contained HTML fragment),
 *       and {@code decoratePage(innerHtml, model)} for the outer-layout pass (wrapping the inner
 *       fragment in the active tenant theme's layout shell). Implementations read theme
 *       configuration from the tenant's active document-theme version and apply it to the
 *       template engine. Falls back to a built-in default layout when no custom theme is
 *       configured for the tenant, ensuring the server is always functional out of the box.</li>
 * </ul>
 *
 * <p><b>Key application services:</b>
 * <ul>
 *   <li>{@code DecorateHtml} — the only application-layer class. Orchestrates the two rendering
 *       passes by injecting the inner HTML (produced by pass 1) as the {@code innerContent}
 *       model variable for pass 2. Called by every HTML driver in the Authorization and Profile
 *       contexts before returning an HTTP response. This design keeps theme logic out of step
 *       handlers and controllers: each step handler produces only its own fragment, and
 *       {@code DecorateHtml} is responsible for composing it into the full page. The method
 *       accepts the logical template name (e.g., {@code "page.login"}), a model map, and the
 *       current request context needed for tenant resolution, and returns the final HTML string
 *       ready to write to the HTTP response.</li>
 * </ul>
 *
 * <p><b>Internal structure:</b>
 * <pre>
 * theme/
 * ├── domain/
 * │   └── gateway/DecoratePageGateway.java     — two-pass rendering port
 * └── application/
 *     └── DecorateHtml.java                    — orchestrator for inner + outer rendering passes
 * </pre>
 *
 * <p><b>Dependencies:</b> No dependency on other OAuth bounded contexts at the domain level.
 * The gateway implementation depends on the document/theme feature outside the OAuth package
 * (tenant configuration, theme versioning, and template storage). Every HTML driver in the
 * Authentication and Profile contexts injects {@code DecorateHtml} and calls it as the final
 * step before returning the HTTP response.
 *
 * <p><b>Design notes:</b> The two-pass model solves the tension between per-step customization
 * (each step has its own template and model) and tenant branding (the outer shell must be
 * consistent). A single-pass approach would require every step template to import or inherit
 * the layout, coupling template authors to the theming infrastructure. The current design
 * allows step templates to be authored as pure HTML fragments ignorant of branding concerns.
 *
 * <p><b>Stability:</b> stable; the two-pass contract is unlikely to change. Evolution typically
 * happens in the gateway implementation (new theme storage backends, new template engines) rather
 * than in the application service or domain model.
 */
package net.civeira.phylax.features.oauth.theme;
