/* @autogenerated */
package net.civeira.phylax.features.oauth.authentication.application.spi;

import java.util.Locale;
import java.util.Optional;

import lombok.Builder;
import lombok.Data;
import net.civeira.phylax.features.oauth.authentication.domain.model.AuthRequest;

public interface UserConsentSpi {
  @Data
  @Builder
  public static class Consent {
    private final String version;
    private final boolean hasChange;
    private final String fullText;
    private final boolean pending;
  }

  Optional<Consent> userRequiredConsent(Locale locale, AuthRequest request, String username);

  boolean confirmConsent(AuthRequest request, String username, String version);
}
