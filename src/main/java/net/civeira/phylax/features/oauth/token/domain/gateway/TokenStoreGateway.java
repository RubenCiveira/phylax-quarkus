/* @autogenerated */
package net.civeira.phylax.features.oauth.token.domain.gateway;

import java.time.Duration;
import java.time.Instant;
import java.util.List;
import java.util.Optional;

import net.civeira.phylax.features.oauth.token.domain.model.KeyInformation;
import net.civeira.phylax.features.oauth.token.domain.model.PublicKeyInformation;

public interface TokenStoreGateway {
  Optional<KeyInformation> currentKey();

  Instant nextKeysExpiration();

  List<PublicKeyInformation> listPublicKeys();

  void saveKey(KeyInformation key, Instant since, Duration caducidad);
}
