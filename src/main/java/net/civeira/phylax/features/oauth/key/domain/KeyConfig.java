/* @autogenerated */
package net.civeira.phylax.features.oauth.key.domain;

import java.time.Duration;

import lombok.Builder;
import lombok.Getter;

/**
 * Configuration for token signing key rotation.
 *
 * Responsibilities: - Define the key time-to-live value. - Define how many future keys to
 * pre-generate.
 *
 * Design notes: - Built via Lombok with sensible defaults. - Used by TokenSigner implementations.
 */
@Builder
@Getter
public class KeyConfig {
  /**
   * Time-to-live for each signing key.
   */
  @Builder.Default
  private final Duration ttl = Duration.ofDays(7);
  /**
   * Number of future keys to generate.
   */
  @Builder.Default
  private final int futures = 3;
}
