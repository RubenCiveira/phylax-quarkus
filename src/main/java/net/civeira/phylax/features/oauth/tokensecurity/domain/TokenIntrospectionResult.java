/* @autogenerated */
package net.civeira.phylax.features.oauth.tokensecurity.domain;

import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonProperty;

import io.quarkus.runtime.annotations.RegisterForReflection;
import lombok.Builder;
import lombok.Data;

/**
 * Result of a token introspection request (RFC 7662).
 *
 * Responsibilities: - Represent the active/inactive status and metadata of an introspected token. -
 * Serialize to the JSON shape mandated by RFC 7662.
 *
 * Design notes: - Null fields are excluded from serialization when the token is inactive. - Built
 * via Lombok builder; use {@link #inactive()} for the inactive sentinel.
 */
@Data
@Builder
@RegisterForReflection
@JsonInclude(Include.NON_NULL)
public class TokenIntrospectionResult {

  /**
   * Whether the token is currently active.
   */
  private final boolean active;

  /**
   * Subject of the token (username / user identifier).
   */
  @JsonProperty("sub")
  private final String sub;

  /**
   * Client identifier that was issued the token.
   */
  @JsonProperty("client_id")
  private final String clientId;

  /**
   * Space-separated list of OAuth scopes associated with the token.
   */
  @JsonProperty("scope")
  private final String scope;

  /**
   * Expiration time in seconds since epoch (Unix timestamp).
   */
  @JsonProperty("exp")
  private final Long exp;

  /**
   * Issued-at time in seconds since epoch (Unix timestamp).
   */
  @JsonProperty("iat")
  private final Long iat;

  /**
   * Issuer URL of the token.
   */
  @JsonProperty("iss")
  private final String iss;

  /**
   * JWT ID (jti) claim of the token.
   */
  @JsonProperty("jti")
  private final String jti;

  /**
   * Returns the inactive sentinel value.
   *
   * Used when a token is expired, revoked, or otherwise invalid. Only the {@code active: false}
   * field is serialized.
   *
   * @return inactive introspection result
   */
  public static TokenIntrospectionResult inactive() {
    return TokenIntrospectionResult.builder().active(false).build();
  }
}
