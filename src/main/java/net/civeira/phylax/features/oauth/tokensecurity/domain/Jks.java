/* @autogenerated */
package net.civeira.phylax.features.oauth.tokensecurity.domain;

import io.quarkus.runtime.annotations.RegisterForReflection;
import lombok.Builder;
import lombok.Data;

/**
 * JSON Web Key (JWK) representation for a single RSA key.
 *
 * Responsibilities: - Hold key parameters used in JWKS responses. - Provide algorithm and usage
 * metadata for clients.
 *
 * Design notes: - Immutable value object built via Lombok. - Used by JWK set generation and JWKS
 * endpoint.
 */
@Data
@Builder
@RegisterForReflection
public class Jks {
  /**
   * Key type, e.g., RSA.
   */
  private final String kty;
  /**
   * Key identifier.
   */
  private final String kid;
  /**
   * RSA modulus parameter.
   */
  private final String n;
  /**
   * RSA exponent parameter.
   */
  private final String e;
  /**
   * Algorithm used for the key.
   */
  private final String alg;
  /**
   * Intended usage of the key.
   */
  private final String use;
}
