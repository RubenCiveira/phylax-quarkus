package net.civeira.phylax.features.notification.smtpoutboundconfig.application.visibility;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class SmtpOutboundConfigVisibilityFilterUnitTest {

  /**
   * @autogenerated ValueObjectGenerator
   */
  @Test
  @DisplayName("Test a entity reference contruction")
  void test_smtp_outbound_config_visibility_filter_builder() {
    SmtpOutboundConfigVisibilityFilter cursor =
        SmtpOutboundConfigVisibilityFilter.builder().build();
    Assertions.assertFalse(cursor.getUid().isPresent(),
        "Since must be empty if cursor is build without it");
    cursor.setUid("one");
    Assertions.assertEquals("one", cursor.getUid().get(),
        "Since Uid must be the same as the assigned");
  }
}
