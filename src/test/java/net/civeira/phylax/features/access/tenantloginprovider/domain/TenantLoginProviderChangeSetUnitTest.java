package net.civeira.phylax.features.access.tenantloginprovider.domain;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import net.civeira.phylax.features.access.tenant.domain.TenantReference;

class TenantLoginProviderChangeSetUnitTest {

  /**
   * @autogenerated CachedGenerator
   */
  @Test
  @DisplayName("Test a entity reference contruction")
  void test_tenant_login_provider_change_set_builder() {
    TenantLoginProviderChangeSet one =
        TenantLoginProviderChangeSet.builder().uid("one").tenant(TenantReference.of("one"))
            .name("one").source(TenantLoginProviderSourceOptions.GOOGLE).disabled(true)
            .directAccess(true).publicKey("one").privateKey("one").certificate("one")
            .metadata("file_1").usersEnabledByDefault(true).version(1).newUid().build();
    one.setUid("two");
    Assertions.assertEquals("two", one.getUid().get().getUid(),
        "Value must equals to the assigned value");
    one.unset("uid");
    Assertions.assertFalse(one.getUid().isPresent(), "After unset, the valeue must be dissapear");
    Assertions.assertEquals(TenantReference.of("one"), one.getTenant().get().getTenant(),
        "Value must equals to the intial value");
    one.setTenant(TenantReference.of("two"));
    Assertions.assertEquals(TenantReference.of("two"), one.getTenant().get().getTenant(),
        "Value must equals to the assigned value");
    one.unset("tenant");
    Assertions.assertFalse(one.getTenant().isPresent(),
        "After unset, the valeue must be dissapear");
    Assertions.assertEquals("one", one.getName().get().getName(),
        "Value must equals to the intial value");
    one.setName("two");
    Assertions.assertEquals("two", one.getName().get().getName(),
        "Value must equals to the assigned value");
    one.unset("name");
    Assertions.assertFalse(one.getName().isPresent(), "After unset, the valeue must be dissapear");
    Assertions.assertEquals(TenantLoginProviderSourceOptions.GOOGLE,
        one.getSource().get().getSource(), "Value must equals to the intial value");
    one.setSource(TenantLoginProviderSourceOptions.GITHUB);
    Assertions.assertEquals(TenantLoginProviderSourceOptions.GITHUB,
        one.getSource().get().getSource(), "Value must equals to the assigned value");
    one.unset("source");
    Assertions.assertFalse(one.getSource().isPresent(),
        "After unset, the valeue must be dissapear");
    Assertions.assertEquals(true, one.getDisabled().get().isDisabled(),
        "Value must equals to the intial value");
    one.setDisabled(false);
    Assertions.assertEquals(false, one.getDisabled().get().isDisabled(),
        "Value must equals to the assigned value");
    one.unset("disabled");
    Assertions.assertFalse(one.getDisabled().isPresent(),
        "After unset, the valeue must be dissapear");
    Assertions.assertEquals(true, one.getDirectAccess().get().isDirectAccess(),
        "Value must equals to the intial value");
    one.setDirectAccess(false);
    Assertions.assertEquals(false, one.getDirectAccess().get().isDirectAccess(),
        "Value must equals to the assigned value");
    one.unset("directAccess");
    Assertions.assertFalse(one.getDirectAccess().isPresent(),
        "After unset, the valeue must be dissapear");
    Assertions.assertEquals("one", one.getPublicKey().get().getPublicKey().get(),
        "Value must equals to the intial value");
    one.setPublicKey("two");
    Assertions.assertEquals("two", one.getPublicKey().get().getPublicKey().get(),
        "Value must equals to the assigned value");
    one.unset("publicKey");
    Assertions.assertFalse(one.getPublicKey().isPresent(),
        "After unset, the valeue must be dissapear");
    Assertions.assertEquals("one", one.getPrivateKey().get().getPrivateKey().get(),
        "Value must equals to the intial value");
    one.setPrivateKey("two");
    Assertions.assertEquals("two", one.getPrivateKey().get().getPrivateKey().get(),
        "Value must equals to the assigned value");
    one.unset("privateKey");
    Assertions.assertFalse(one.getPrivateKey().isPresent(),
        "After unset, the valeue must be dissapear");
    Assertions.assertEquals("one", one.getCertificate().get().getCertificate().get(),
        "Value must equals to the intial value");
    one.setCertificate("two");
    Assertions.assertEquals("two", one.getCertificate().get().getCertificate().get(),
        "Value must equals to the assigned value");
    one.unset("certificate");
    Assertions.assertFalse(one.getCertificate().isPresent(),
        "After unset, the valeue must be dissapear");
    Assertions.assertEquals("file_1", one.getMetadata().get().getMetadata().get(),
        "Value must equals to the intial value");
    one.setMetadata("file_2");
    Assertions.assertEquals("file_2", one.getMetadata().get().getMetadata().get(),
        "Value must equals to the assigned value");
    one.unset("metadata");
    Assertions.assertFalse(one.getMetadata().isPresent(),
        "After unset, the valeue must be dissapear");
    Assertions.assertEquals(true, one.getUsersEnabledByDefault().get().isUsersEnabledByDefault(),
        "Value must equals to the intial value");
    one.setUsersEnabledByDefault(false);
    Assertions.assertEquals(false, one.getUsersEnabledByDefault().get().isUsersEnabledByDefault(),
        "Value must equals to the assigned value");
    one.unset("usersEnabledByDefault");
    Assertions.assertFalse(one.getUsersEnabledByDefault().isPresent(),
        "After unset, the valeue must be dissapear");
    Assertions.assertEquals(0, one.getVersion().get().getVersion().get(),
        "Value must equals to the intial value");
    one.setVersion(2);
    Assertions.assertEquals(2, one.getVersion().get().getVersion().get(),
        "Value must equals to the assigned value");
    one.unset("version");
    Assertions.assertFalse(one.getVersion().isPresent(),
        "After unset, the valeue must be dissapear");
  }
}
