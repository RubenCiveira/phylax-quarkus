package net.civeira.phylax.features.access.tenant.domain.gateway;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class TenantOrderUnitTest {

  /**
   * @autogenerated ValueObjectGenerator
   */
  @Test
  @DisplayName("Test a entity reference contruction")
  void test_tenant_order_builder() {
    Assertions.assertNotNull(TenantOrder.NAME_ASC, "Since Uid must be the same as the assigned");
    Assertions.assertNotNull(TenantOrder.NAME_DESC, "Since Uid must be the same as the assigned");
    Assertions.assertNotNull(TenantOrder.DOMAIN_ASC, "Since Uid must be the same as the assigned");
    Assertions.assertNotNull(TenantOrder.DOMAIN_DESC, "Since Uid must be the same as the assigned");
  }
}
