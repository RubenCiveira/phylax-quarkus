package net.civeira.phylax.features.access.userconsentpurposes.domain.gateway;

import java.util.List;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import net.civeira.phylax.features.access.consentpurpose.domain.ConsentPurposeReference;
import net.civeira.phylax.features.access.user.domain.UserReference;

class UserConsentPurposesFilterUnitTest {

  /**
   * @autogenerated ValueObjectGenerator
   */
  @Test
  @DisplayName("Test a entity reference contruction")
  void test_user_consent_purposes_filter_builder() {
    UserConsentPurposesFilter filter = UserConsentPurposesFilter.builder().build();
    Assertions.assertFalse(filter.getUid().isPresent(),
        "Uid must be empty if filter is build without it");
    filter.setUid("one");
    Assertions.assertEquals("one", filter.getUid().get(), "Uid must be the same as the assigned");
    Assertions.assertTrue(filter.getUids().isEmpty(),
        "Uid must be empty if filter is build without it");
    filter.setUids(List.of("one"));
    Assertions.assertEquals(List.of("one"), filter.getUids(),
        "Uids must be the same as the assigned");
    Assertions.assertFalse(filter.getSearch().isPresent(),
        "Search must be empty if filter is build without it");
    filter.setSearch("lookup");
    Assertions.assertEquals("lookup", filter.getSearch().get(),
        "Search must be the same as the assigned");
    Assertions.assertFalse(filter.getUser().isPresent(),
        "User must be empty if filter is build without it");
    filter.setUser(UserReference.of("$ref"));
    Assertions.assertEquals(UserReference.of("$ref"), filter.getUser().get(),
        "User must be the same as the assigned");
    Assertions.assertTrue(filter.getUsers().isEmpty(),
        "Users must be empty if filter is build without it");
    filter.setUsers(List.of("one"));
    Assertions.assertEquals(List.of("one"), filter.getUsers(),
        "Users must be the same as the assigned");
    Assertions.assertFalse(filter.getConsentPurpose().isPresent(),
        "ConsentPurpose must be empty if filter is build without it");
    filter.setConsentPurpose(ConsentPurposeReference.of("$ref"));
    Assertions.assertEquals(ConsentPurposeReference.of("$ref"), filter.getConsentPurpose().get(),
        "ConsentPurpose must be the same as the assigned");
    Assertions.assertTrue(filter.getConsentPurposes().isEmpty(),
        "ConsentPurposes must be empty if filter is build without it");
    filter.setConsentPurposes(List.of("one"));
    Assertions.assertEquals(List.of("one"), filter.getConsentPurposes(),
        "ConsentPurposes must be the same as the assigned");
    Assertions.assertFalse(filter.getUserTenantAccesible().isPresent(),
        "UserTenantAccesible must be empty if filter is build without it");
    filter.setUserTenantAccesible("one");
    Assertions.assertEquals("one", filter.getUserTenantAccesible().get(),
        "UserTenantAccesible must be the same as the assigned");
  }
}
