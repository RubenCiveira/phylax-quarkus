/* @autogenerated */
package net.civeira.phylax.features.oauth.maintenance;

import static org.mockito.Mockito.doThrow;
import static org.mockito.Mockito.verify;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

/**
 * Unit tests for {@link OAuthExpiredRecordsCleaner}.
 */
class OAuthExpiredRecordsCleanerTest {

  @Mock
  private OAuthCleanupService cleanupService;

  @InjectMocks
  private OAuthExpiredRecordsCleaner cleaner;

  @BeforeEach
  void setUp() {
    MockitoAnnotations.openMocks(this);
  }

  @Test
  @DisplayName("cleanExpiredTemporalCodes() delegates to OAuthCleanupService")
  void cleanExpiredTemporalCodes_delegatesToService() {
    cleaner.cleanExpiredTemporalCodes();

    verify(cleanupService).deleteExpiredTemporalCodes();
  }

  @Test
  @DisplayName("cleanExpiredTemporalCodes() swallows exceptions thrown by the service")
  void cleanExpiredTemporalCodes_swallowsException() {
    doThrow(new IllegalStateException("DB failure")).when(cleanupService)
        .deleteExpiredTemporalCodes();

    cleaner.cleanExpiredTemporalCodes();

    verify(cleanupService).deleteExpiredTemporalCodes();
  }

  @Test
  @DisplayName("cleanExpiredSessions() delegates to OAuthCleanupService")
  void cleanExpiredSessions_delegatesToService() {
    cleaner.cleanExpiredSessions();

    verify(cleanupService).deleteExpiredSessions();
  }

  @Test
  @DisplayName("cleanExpiredSessions() swallows exceptions thrown by the service")
  void cleanExpiredSessions_swallowsException() {
    doThrow(new IllegalStateException("DB failure")).when(cleanupService).deleteExpiredSessions();

    cleaner.cleanExpiredSessions();

    verify(cleanupService).deleteExpiredSessions();
  }
}
