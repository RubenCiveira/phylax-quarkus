package net.civeira.phylax.features.document.snippet.domain.gateway;

import java.time.OffsetDateTime;
import java.util.List;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import net.civeira.phylax.features.access.tenant.domain.TenantReference;
import net.civeira.phylax.features.document.snippet.domain.Snippet;
import net.civeira.phylax.features.document.snippet.domain.valueobject.CodeVO;
import net.civeira.phylax.features.document.snippet.domain.valueobject.EnabledVO;
import net.civeira.phylax.features.document.snippet.domain.valueobject.TenantVO;
import net.civeira.phylax.features.document.snippet.domain.valueobject.UidVO;
import net.civeira.phylax.features.document.snippet.domain.valueobject.VersionVO;

class SnippetCachedUnitTest {

  /**
   * @autogenerated CachedGenerator
   */
  @Test
  @DisplayName("Test a entity reference contruction")
  void test_snippet_cached_builder() {
    Snippet one = Snippet.builder().uidValue(UidVO.from("one")).codeValue(CodeVO.from("one"))
        .tenantValue(TenantVO.from(TenantReference.of("one"))).enabledValue(EnabledVO.from(true))
        .versionValue(VersionVO.from(1)).build();
    OffsetDateTime now = OffsetDateTime.now();
    List<Snippet> empty = List.of();
    List<Snippet> some = List.of(one);
    SnippetCached emptyCursor = SnippetCached.builder().since(now).value(empty).build();
    Assertions.assertFalse(emptyCursor.first().isPresent(), "An empty list dont have first");
    SnippetCached someCursor = SnippetCached.builder().since(now).value(some).build();
    Assertions.assertEquals(one, someCursor.first().get(),
        "A list with value must retrieve firt value");
  }
}
