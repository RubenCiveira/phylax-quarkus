package net.civeira.phylax.features.document.themeversion.domain.gateway;

import java.util.List;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import net.civeira.phylax.features.document.theme.domain.ThemeReference;

class ThemeVersionFilterUnitTest {

  /**
   * @autogenerated ValueObjectGenerator
   */
  @Test
  @DisplayName("Test a entity reference contruction")
  void test_theme_version_filter_builder() {
    ThemeVersionFilter filter = ThemeVersionFilter.builder().build();
    Assertions.assertFalse(filter.getUid().isPresent(),
        "Uid must be empty if filter is build without it");
    filter.setUid("one");
    Assertions.assertEquals("one", filter.getUid().get(), "Uid must be the same as the assigned");
    Assertions.assertTrue(filter.getUids().isEmpty(),
        "Uid must be empty if filter is build without it");
    filter.setUids(List.of("one"));
    Assertions.assertEquals(List.of("one"), filter.getUids(),
        "Uids must be the same as the assigned");
    Assertions.assertFalse(filter.getSearch().isPresent(),
        "Search must be empty if filter is build without it");
    filter.setSearch("lookup");
    Assertions.assertEquals("lookup", filter.getSearch().get(),
        "Search must be the same as the assigned");
    Assertions.assertFalse(filter.getTheme().isPresent(),
        "Theme must be empty if filter is build without it");
    filter.setTheme(ThemeReference.of("$ref"));
    Assertions.assertEquals(ThemeReference.of("$ref"), filter.getTheme().get(),
        "Theme must be the same as the assigned");
    Assertions.assertTrue(filter.getThemes().isEmpty(),
        "Themes must be empty if filter is build without it");
    filter.setThemes(List.of("one"));
    Assertions.assertEquals(List.of("one"), filter.getThemes(),
        "Themes must be the same as the assigned");
    Assertions.assertFalse(filter.getThemeTenantAccesible().isPresent(),
        "ThemeTenantAccesible must be empty if filter is build without it");
    filter.setThemeTenantAccesible("one");
    Assertions.assertEquals("one", filter.getThemeTenantAccesible().get(),
        "ThemeTenantAccesible must be the same as the assigned");
  }
}
