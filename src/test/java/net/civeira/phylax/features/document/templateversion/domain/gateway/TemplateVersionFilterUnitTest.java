package net.civeira.phylax.features.document.templateversion.domain.gateway;

import java.util.List;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import net.civeira.phylax.features.document.template.domain.TemplateReference;

class TemplateVersionFilterUnitTest {

  /**
   * @autogenerated ValueObjectGenerator
   */
  @Test
  @DisplayName("Test a entity reference contruction")
  void test_template_version_filter_builder() {
    TemplateVersionFilter filter = TemplateVersionFilter.builder().build();
    Assertions.assertFalse(filter.getUid().isPresent(),
        "Uid must be empty if filter is build without it");
    filter.setUid("one");
    Assertions.assertEquals("one", filter.getUid().get(), "Uid must be the same as the assigned");
    Assertions.assertTrue(filter.getUids().isEmpty(),
        "Uid must be empty if filter is build without it");
    filter.setUids(List.of("one"));
    Assertions.assertEquals(List.of("one"), filter.getUids(),
        "Uids must be the same as the assigned");
    Assertions.assertFalse(filter.getSearch().isPresent(),
        "Search must be empty if filter is build without it");
    filter.setSearch("lookup");
    Assertions.assertEquals("lookup", filter.getSearch().get(),
        "Search must be the same as the assigned");
    Assertions.assertFalse(filter.getTemplate().isPresent(),
        "Template must be empty if filter is build without it");
    filter.setTemplate(TemplateReference.of("$ref"));
    Assertions.assertEquals(TemplateReference.of("$ref"), filter.getTemplate().get(),
        "Template must be the same as the assigned");
    Assertions.assertTrue(filter.getTemplates().isEmpty(),
        "Templates must be empty if filter is build without it");
    filter.setTemplates(List.of("one"));
    Assertions.assertEquals(List.of("one"), filter.getTemplates(),
        "Templates must be the same as the assigned");
    Assertions.assertFalse(filter.getTemplateTenantTenantAccesible().isPresent(),
        "TemplateTenantTenantAccesible must be empty if filter is build without it");
    filter.setTemplateTenantTenantAccesible("one");
    Assertions.assertEquals("one", filter.getTemplateTenantTenantAccesible().get(),
        "TemplateTenantTenantAccesible must be the same as the assigned");
  }
}
