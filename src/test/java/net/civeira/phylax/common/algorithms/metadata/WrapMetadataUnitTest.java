/* @autogenerated */
package net.civeira.phylax.common.algorithms.metadata;

import java.time.Instant;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

class WrapMetadataUnitTest {

  @Test
  void testBuild() {
    Instant instant = Instant.now();
    WrapMetadata<String> wrapped =
        WrapMetadata.<String>builder().data("verde").since(instant).build();

    Assertions.assertEquals("verde", wrapped.getData());
    Assertions.assertEquals(instant, wrapped.getSince());
  }
}
